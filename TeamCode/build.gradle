//
// build.gradle in TeamCode
//
// Most of the definitions for building your module reside in a common, shared
// file 'build.common.gradle'. Being factored in this way makes it easier to
// integrate updates to the FTC into your code. If you really need to customize
// the build definitions, you can place those customizations in this file, but
// please think carefully as to whether such customizations are really necessary
// before doing so.


// Custom definitions may go here

// Include common definitions from above.
apply from: '../build.common.gradle'
apply from: '../build.dependencies.gradle'

android {
    namespace = 'org.firstinspires.ftc.teamcode'

    packagingOptions {
        jniLibs.useLegacyPackaging true
    }

    testOptions {
        unitTests {
            // Android スタブ JAR に含まれない API 呼び出し
            // (android.opengl.Matrix.setIdentityM など) が「not mocked」例外を投げる代わりに
            // デフォルト値 (0 / null / empty) を返すようにする。
            // ShooterSubsystem の RevHubOrientationOnRobot 初期化で必要。
            returnDefaultValues = true
        }
    }
}

dependencies {
    implementation project(':FtcRobotController')
    implementation 'org.apache.commons:commons-math3:3.6.1'
    implementation 'com.fasterxml.jackson.core:jackson-databind:2.20.1'
    implementation 'dev.nextftc:ftc:1.0.1'
    implementation 'dev.nextftc:hardware:1.0.1' // If you would like to use the hardware module
    implementation 'dev.nextftc.extensions:pedro:1.0.0'
    implementation 'dev.nextftc:hardware:1.0.1'
    implementation 'dev.nextftc:control:1.0.0'

    // ===== Unit testing =====
    // JUnit 4 (FTC SDK は JUnit 4 系がトラブルが少ない)
    testImplementation 'junit:junit:4.13.2'
    // Mockito 5.x: mockito-core が inline mock maker をデフォルトで提供。
    // mockito-inline パッケージは 5.2.0 で廃止されたため使わない。
    testImplementation 'org.mockito:mockito-core:5.14.2'
}
